# Plan: Scrape Mayar.id Payment Gateway Documentation

## Goal
Scrape the complete API documentation for "Mayar.id" from `https://docs.mayar.id/api-reference` using the Firecrawl MCP and save it to the project for reference.

## Phase 1: Exploration & Validation
- [x] Map the target website (`https://docs.mayar.id/api-reference`) to understand its structure and volume of pages.
  - Result: It is a multi-page site with ~58 URLs covering various endpoints (invoices, customers, webhooks, etc.).
- [x] Determine if the documentation is a single-page application (SPA) or multi-page site to choose the best scraping strategy (crawl vs. single page scrape).
  - Result: Multi-page.

## Phase 2: Implementation
- [x] Create a directory `docs/mayar-api` to store the documentation.
- [x] Use `firecrawl_crawl` to scrape the mapped URLs.
  - Target URL: `https://docs.mayar.id/api-reference/*`
  - Limit: 100 (to cover the ~60 pages safe margin)
  - Formats: Markdown
- [x] Consolidate the scraped content into a single file `docs/mayar-api/complete-reference.md` for easier RAG/context usage.

## Phase 3: Verification
- [x] Check `docs/mayar-api/complete-reference.md` to ensure it contains key sections like Authentication, Invoice creation, and Webhooks.
