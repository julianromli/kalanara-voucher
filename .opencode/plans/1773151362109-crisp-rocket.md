# Admin Invite + Password Setup Plan

## Recommended Approach
Implement dual onboarding for new admin users, with `email invite` as the default and `manual password` as an optional alternative. Keep `public.admins` as the source of truth for role access. Use a public auth handoff under `/auth/*` so invite acceptance and password setup do not conflict with the existing `/admin/*` middleware.

## Scope
- Add onboarding mode selection to the admin user creation dialog.
- Support both:
  - `invite`: send Supabase invite email with a redirect into the app.
  - `manual`: create the auth user with a provided password.
- Add a public callback + set-password flow.
- After invite password setup succeeds, auto-enter the admin dashboard if the user still has a valid admin role.

## Files To Modify
- `components/admin/admin-users-client.tsx`
  - Add onboarding mode control.
  - Add conditional `password` and `confirm password` fields for manual mode.
  - Update success and error messages for invite vs manual creation.
- `lib/actions/admin-users.ts`
  - Extend `createAdminUser` input to include onboarding mode and optional password.
  - Replace temp-password-only flow with two branches:
    - `inviteUserByEmail(email, { redirectTo })`
    - `createUser({ email, password, email_confirm: true })`
  - Keep `admins` insert and metadata sync.
  - Return mode-aware result for the UI.
- `app/admin/login/page.tsx`
  - Read auth-related query params.
  - Show onboarding/invite error states and guidance.
  - Add copy telling invited admins to use the email link first.

## Files To Add
- `app/auth/callback/route.ts`
  - Exchange Supabase auth code for session.
  - Validate the signed-in user against `public.admins`.
  - Redirect invited users to `/auth/set-password`.
  - Redirect failures to `/admin/login` with a reason in query params.
- `app/auth/set-password/page.tsx`
  - Public password setup page for invited admins.
  - Require and validate password + confirm password.
  - Call `supabase.auth.updateUser({ password })`.
  - After success, verify admin access and redirect to `/admin/dashboard`.
- Optional: `components/auth/admin-set-password-form.tsx`
  - Extract the password setup UI if the page gets too large.

## Implementation Notes
- Do not place invite or password-setup pages under `/admin/*`; keep them under `/auth/*` to avoid the current middleware redirect in `lib/supabase/middleware.ts`.
- Build invite `redirectTo` from `NEXT_PUBLIC_APP_URL`.
- Continue using `public.admins` as the authorization source; auth metadata remains best-effort only.
- No database migration is required unless invite lifecycle tracking is later needed.
- Preserve current permission checks: only users with `users.manage` can create admin accounts.

## Validation Rules
- `invite` mode is the default in the dialog.
- `manual` mode requires non-empty password and confirm password.
- Prevent submit when password confirmation mismatches.
- Keep role selection unchanged from the current admin flow.

## Failure Handling
- If auth user creation/invite succeeds but `admins` insert fails, delete the created auth user.
- Handle duplicate email/admin cases with clear UI errors.
- Handle expired, reused, or invalid invite links on `/auth/set-password`.
- If a signed-in invited user no longer has a valid `admins` row, sign them out and send them to `/admin/login?error=no_admin_access`.
- Never log or expose manual passwords.

## Verification
1. Run `bunx tsc --noEmit`.
2. Run `bun run lint`.
3. Manual test invite flow:
   - Create admin in invite mode.
   - Receive Supabase invite email.
   - Click link.
   - Land on `/auth/set-password`.
   - Set password successfully.
   - Auto-enter `/admin/dashboard` with the assigned role.
4. Manual test manual-password flow:
   - Create admin in manual mode.
   - Login at `/admin/login` with the provided password.
   - Confirm access matches the assigned role.
5. Regression test:
   - Existing admin login still works.
   - Unauthenticated users are still redirected away from protected `/admin/*` routes.
   - Non-admin authenticated users still cannot keep admin access.

## Suggested Tests
- Update/add server action tests around `lib/actions/admin-users.ts` for both onboarding modes, duplicate handling, cleanup, and role persistence.
- Add tests for the new auth callback/set-password flow if test coverage is expanded in this area.
- Extend admin user UI tests if the skipped `components/__tests__/admin-users-client.test.tsx` is revived.